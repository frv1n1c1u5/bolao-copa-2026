# Conexão Postgres (Vercel Postgres / Neon)
DATABASE_URL=postgresql://user:pass@host/db?sslmode=require

# Segredo da sessão (gere com: openssl rand -hex 32)
SESSION_SECRET=troque-este-valor

# Opcional: chave do football-data.org para o botão "Sincronizar" do admin
FOOTBALL_DATA_API_KEY=
